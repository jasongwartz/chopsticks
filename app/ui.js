// Generated by CoffeeScript 1.10.0
$('#bdbutton').on('click', function() {
  var $btn, i;
  $btn = $(this).button('loading');
  i = instruments.find(function(ins) {
    return ins.name === 'bd';
  });
  i.is_live = !i.is_live;
  return $btn.button('reset');
});

$('#sdbutton').on('click', function() {
  var $btn, i;
  $btn = $(this).button('loading');
  i = instruments.find(function(ins) {
    return ins.name === 'sd';
  });
  i.is_live = !i.is_live;
  return $btn.button('reset');
});

$('#cymbutton').on('click', function() {
  var $btn, i;
  $btn = $(this).button('loading');
  i = instruments.find(function(ins) {
    return ins.name === 'cym';
  });
  i.is_live = !i.is_live;
  return $btn.button('reset');
});
